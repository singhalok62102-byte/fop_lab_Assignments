#include <stdio.h>
int fact(int n){
    if(n==0 || n==1) return 1;
    return n*fact(n-1);
}

int main(){
    int n,i,f=1;
    scanf("%d",&n);

    for(i=1;i<=n;i++)
        f*=i;

    printf("Iterative = %d\n",f);
    printf("Recursive = %d",fact(n));

    return 0;
}
